public class Tool {
    ToolCode toolCode;
    ToolType toolType;
    ToolBrand brand;

    public Tool(ToolCode toolCode, ToolType toolType, ToolBrand brand) {
        this.toolCode = toolCode;
        this.toolType = toolType;
        this.brand = brand;
    }
}
