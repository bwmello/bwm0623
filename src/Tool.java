public class Tool {
    String toolCode;
    ToolType toolType;
    ToolBrand brand;

    public Tool(String toolCode, ToolType toolType, ToolBrand brand) {
        this.toolCode = toolCode;
        this.toolType = toolType;
        this.brand = brand;
    }
}
